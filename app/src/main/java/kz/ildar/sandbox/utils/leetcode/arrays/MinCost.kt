package kz.ildar.sandbox.utils.leetcode.arrays

/**
 * find the minimum cost required to make each element of every subarray of length K equal.
 * Cost of replacing any array element by another element
 * is the absolute difference between the two.
 *
 * Input: [array] = [1, 2, 3, 4, 6], [k] = 3
 * Output: 7
 * Subarray 1: Cost to convert subarray {1, 2, 3} to {2, 2, 2} = |1-2| + |2-2| + |3-2| = 2
 */
fun minCost(array: IntArray, k: Int): Int {
    if (k > array.size) return -1

    var totalCost = 0


    return 0
}

private fun calcCost() :Int{
    return 0
}